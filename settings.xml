<settings>
  <servers>
    <server>
      <id>mulesoft-private</id>
      <username>${env.MULESOFT_USERNAME}</username>
      <password>${env.MULESOFT_PASSWORD}</password>
    </server>
    <server>
      <id>github.com</id>
      <username>${env.GITHUB_USERNAME}</username>
      <password>${env.GITHUB_PASSWORD}</password>
    </server>
  </servers>
</settings>
